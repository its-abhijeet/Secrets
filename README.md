# Secrets
An Anonymous Secrets sharing App.
# Usage
Run npm init to install the files.
Then 'nodemon app.js' to fire it locally.
#P.S
Couldn't get the time to host it somewhere but hope it doesn't bother.
